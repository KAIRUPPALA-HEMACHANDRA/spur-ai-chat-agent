



The LLM provider is abstracted behind an LLMProvider interface.

The current implementation uses Gemini, but additional providers such as OpenAI or Claude can be added without changing business logic.